components:
  schemas:
    Colors:
      enum:
      - BLACK
      - RED
      - ORANGE
      - YELLOW
      - BLUE
      - GREEN
      type: string
    EmbeddedEnumArray:
      properties:
        colors:
          items:
            enum:
            - BLACK
            - RED
            - ORANGE
            - YELLOW
            - BLUE
            - GREEN
            type: string
          type: array
        reqColors:
          items:
            enum:
            - BLACK
            - RED
            - ORANGE
            - YELLOW
            - BLUE
            - GREEN
            type: string
          type: array
      required:
      - reqColors
      type: object
    NoEmbeddedEnumArray:
      properties:
        colors:
          items:
            $ref: '#/components/schemas/Colors'
          type: array
        reqColors:
          items:
            $ref: '#/components/schemas/Colors'
          required:
          - reqColors
          type: array
      type: object
    SimpleColorContainer:
      properties:
        color:
          enum:
          - BLACK
          - RED
          - ORANGE
          - YELLOW
          - BLUE
          - GREEN
          type: string
        reqColor:
          enum:
          - BLACK
          - RED
          - ORANGE
          - YELLOW
          - BLUE
          - GREEN
          type: string
      required:
      - reqColor
      type: object
info:
  title: test
  version: '1.0'
openapi: 3.0.1
paths:
  /test:
    get:
      operationId: test
      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmbeddedEnumArray'
          description: OK

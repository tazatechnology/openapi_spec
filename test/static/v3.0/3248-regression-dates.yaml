info:
  description: info
  title: info
  version: 0.1.0
openapi: 3.0.2
paths:
  /example/api:
    get:
      description: description
      parameters:
      - description: The start time.
        in: query
        name: start
        required: true
        schema:
          format: date-time
          type: string
      responses:
        200:
          content:
            application/json:
              schema:
                type: string
          description: response
      summary: summary

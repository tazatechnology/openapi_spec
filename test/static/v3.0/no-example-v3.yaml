info:
  title: Regression test for an API which doesn't have any example
  version: 0.0.1
openapi: 3.0.1
paths:
  /op:
    get:
      requestBody:
        content:
          application/json:
            schema:
              properties:
                property:
                  type: string
              required:
              - property
              type: object
        required: true
      responses:
        '200':
          description: OK

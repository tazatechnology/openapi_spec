components:
  schemas:
    Bar:
      properties:
        baz:
          $ref: '#/components/schemas/Baz'
      type: object
    Baz:
      properties:
        bar:
          $ref: '#/components/schemas/Bar'
      type: object
    Foo:
      properties:
        foo:
          $ref: '#/components/schemas/Foo'
      type: object
info:
  description: Test
  title: OpenAPI
  version: 1.0.0
openapi: 3.0.0
paths:
  /foo:
    post:
      description: ''
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Foo'
          description: Response

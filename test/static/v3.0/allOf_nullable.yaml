components:
  schemas:
    Child:
      properties:
        name:
          type: string
      type: object
    Example:
      properties:
        child:
          allOf:
          - $ref: '#/components/schemas/Child'
          nullable: true
      type: object
info:
  license:
    name: MIT
  title: Example
  version: 1.0.0
openapi: 3.0.1
paths:
  /example:
    get:
      operationId: list
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Example'
          description: OK
servers:
- url: http://api.example.xyz/v1

components:
  schemas:
    banana:
      properties:
        count:
          exclusiveMaximum: true
          exclusiveMinimum: true
          maximum: 100
          minimum: 10
          multipleOf: 5
          type: integer
      title: banana
info:
  title: fruity
  version: 0.0.1
openapi: 3.0.1
paths:
  /:
    get:
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/banana'
          description: desc

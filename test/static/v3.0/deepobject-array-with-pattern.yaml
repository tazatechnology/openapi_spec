components:
  schemas:
    Options:
      properties:
        a:
          items:
            pattern: ^[A-Z].*
            type: string
          type: array
      type: object
info:
  title: deepobject-array-with-pattern-test
  version: 1.0.0
openapi: 3.0.3
paths:
  /test:
    get:
      operationId: test
      parameters:
      - explode: true
        in: query
        name: options
        required: false
        schema:
          $ref: '#/components/schemas/Options'
        style: deepObject
      responses:
        '200':
          content:
            text/plain:
              schema:
                type: string
          description: OK

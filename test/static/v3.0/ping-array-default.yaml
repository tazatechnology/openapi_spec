components:
  schemas:
    AnArrayOfString:
      properties:
        arrayWithADefaultValue:
          default:
          - aString
          items:
            type: string
          type: array
      type: object
info:
  title: ping test
  version: '1.0'
openapi: 3.0.1
paths:
  /ping:
    get:
      operationId: pingGet
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnArrayOfString'
          description: OK
servers:
- url: http://localhost:8000/
